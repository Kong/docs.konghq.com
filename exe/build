#!/usr/bin/env bash

set -e

if [ "$LOCALE" = "ja" ]; then
    echo "Building site in japanese..."
    bundle exec jekyll build --config jekyll.yml,_translated_content/jekyll.yml --profile
else
    echo "Building site in english..."
    bundle exec jekyll build --config jekyll.yml --profile
fi
