describe "/mesh/latest_version", type: :request do
  it "has cors headers on latest_version" do
    # Ignore this test until we run netlifydev
    # get "/mesh/latest_version"
    # expect(response.headers).to have_key("Access-Control-Allow-Origin")
    # expect(response.headers["Access-Control-Allow-Origin"]).to eq("*")

    # expect(response.content_type).to eq("text/plain")
  end
end
