# Duplicate this file into your own plugin doc directory under app/_hub/.
# If adding a Kong-owned plugin, add it to app/_hub/kong-inc/<plugin-name>.

strategy: gateway

releases:
# Array of Kong Gateway releases that this plugin is compatible with.
# If adding a new plugin for a specific release, only list that release.
# For example:
  - 3.0.x
