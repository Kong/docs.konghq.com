# Configure timeline of doc articles here
# Format:
#   slug: <article's slug>
#   aliases: list of article's renamed slugs (if any)
#   timeline:
#     <release_version>: <slug_override>
#
# if override value is PAGE_CREATED, all versions below that will be set to "unavailable"
# if override value is PAGE_DELETED, all versions above that will be set to "unavailable"

-
  slug: "getting-started/configuring-a-service"
  aliases: ["getting-started/adding-your-api"]
  timeline:
    0.12.x: "getting-started/adding-your-api"
-
  slug: "clustering"
  timeline:
    0.6.x:  "PAGE_CREATED"
-
  slug: "proxy"
  timeline:
    0.4.x:  "PAGE_CREATED"
